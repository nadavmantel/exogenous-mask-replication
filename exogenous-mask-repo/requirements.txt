# Core dependencies
pypsa>=0.26.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Solver (HiGHS is open-source and fast)
highspy>=1.5.0

# Optional: for better progress bars in long runs
tqdm>=4.65.0
